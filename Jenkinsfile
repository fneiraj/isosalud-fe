pipeline {
    agent {
        docker {
            image '16-alpine3.13' 
            args '-p 3000:3000' 
        }
    }
    stages {
        stage('Build') { 
            steps {
                sh 'npm install' 
            }
        }
    }
}